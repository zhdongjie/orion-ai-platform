services:
  orion-pg:
    image: pgvector/pgvector:pg16
    container_name: zentrio-postgres
    restart: always
    environment:
      POSTGRES_DB: orion_ai_db
      POSTGRES_USER: orion_admin
      POSTGRES_PASSWORD: orion_password_2025
    ports:
      - "5432:5432"
    volumes:
      - ./docker/data/pgsql:/var/lib/postgresql/data
      - ./docker/init/pgsql/init_schema.sql:/docker-entrypoint-initdb.d/init_schema.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U orion_admin -d orion_ai_db" ]
      interval: 10s
      timeout: 5s
      retries: 5